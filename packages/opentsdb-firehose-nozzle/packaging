set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang1.4)
export PATH=$GOROOT/bin:$PATH

cd datadog-firehose-nozzle

export GOPATH=$PWD

go install github.com/pivotal-cloudops/opentsdb-firehose-nozzle-release
cp -a $PWD/bin/opentsdb-firehose-nozzle ${BOSH_INSTALL_TARGET}
